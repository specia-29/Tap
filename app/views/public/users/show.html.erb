<div class="container">
  <div class="row">
    <%= form_with model: @user, url: public_article_path do |f| %>
      <div class="col-lg-9">
        <% if @user.profile_image.attached? %>
          <%= image_tag @user.profile_image %>
        <% end %>
      </div>
aaaaa
  </div>
</div>
<% end %>